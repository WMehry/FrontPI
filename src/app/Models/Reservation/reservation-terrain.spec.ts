import { ReservationTerrain } from './reservation-terrain';

describe('ReservationTerrain', () => {
  it('should create an instance', () => {
    expect(new ReservationTerrain()).toBeTruthy();
  });
});
